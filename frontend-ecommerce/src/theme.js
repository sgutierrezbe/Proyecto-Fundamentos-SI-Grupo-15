import { createTheme } from "@mantine/core";

export const theme = createTheme({
  fontFamily: "Open Sans, sans-serif",
  cursorType: "pointer",
  primaryColor: "red",
  autoContrast: true,
  colors: {
    primary: [
      "#ffe9e9",
      "#ffd1d1",
      "#fba0a1",
      "#f76d6d",
      "#f34141",
      "#f22625",
      "#f21616",
      "#d8070b",
      "#c10008",
      "#a90003",
    ],
  },
});
